<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.com.ham.dtogo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DTOGO"
        android:name=".AppIcon">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Intent1_save">
        </activity>
        <activity android:name=".Intent2" />
        <activity android:name=".Intent1_show"/>
        <activity android:name=".Intent3_save"/>
        <activity android:name=".Intent3_show"/>
        <activity android:name=".Intent1_popup" android:theme="@style/popupTheme"
            android:screenOrientation="portrait"/>
        <activity android:name=".Intent3_popup" android:theme="@style/popupTheme"
            android:screenOrientation="portrait"/>
        <activity android:name=".Main_popup" android:theme="@style/popupThemeXY"
            android:screenOrientation="portrait"/>

    </application>

</manifest>